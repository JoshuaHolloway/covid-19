clc, clear, close all;

x = -20:1.0:20;
y = logsig(x) + 0.05*randn(1,length(x));
figure(1), 
subplot(2,1,1),
scatter(x, y)

% -1 for the zero value
N = length(y) - 1;
N_2 = N/2;

y_half = y(1 : N_2);
x_half = x(1 : N_2);

figure(1),
subplot(2,1,2),
scatter(x_half, y_half)