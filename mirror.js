// ==============================================
const mirror = x => {
    const N = x.length;
    for (let i = 0; i <= Math.ceil(N/2); i++) {
        x[N+i] = x[(N-1)+i] + (x[(N-1)-i] - x[(N-2)-i]);
    }
    return x;
};
// ==============================================
